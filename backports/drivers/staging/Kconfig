# Staging drivers configuration
# This will be populated by gentree.py from upstream kernel

menuconfig CPTCFG_STAGING
	bool "Staging drivers"
	depends on BACKPORT_LINUX
	help
	  This option allows you to select a number of drivers that are
	  not being considered ready for mainline inclusion yet. If you
	  do not plan to use or test these drivers, say N here.

if CPTCFG_STAGING

# Staging drivers will be added here by gentree.py

endif # CPTCFG_STAGING
